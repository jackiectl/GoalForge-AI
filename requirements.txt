# The Vercel serverless functions (api/*.py: health, teams, squad, predict) use only the Python
# standard library, so there is nothing to install here. Keeping this file empty (no fastapi) is
# deliberate: it stops Vercel from auto-detecting a Python/FastAPI backend and looking for a single
# ASGI entrypoint. Full dev / training dependencies: requirements-dev.txt / environment.yml.
